{% extends 'base.html.twig' %}

{% block title %}Gestion des catégories{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>🏷️ Gestion des catégories</h1>
    <div>
        <a href="{{ path('app_admin_categories_new') }}" class="btn btn-success">
            ➕ Nouvelle catégorie
        </a>
        <a href="{{ path('app_admin_index') }}" class="btn btn-outline-secondary">
            ← Retour au tableau de bord
        </a>
    </div>
</div>

{% if categories is empty %}
    <div class="alert alert-info">
        <h4>Aucune catégorie</h4>
        <p>Il n'y a actuellement aucune catégorie dans le système.</p>
        <a href="{{ path('app_admin_categories_new') }}" class="btn btn-primary">Créer la première catégorie</a>
    </div>
{% else %}
    <div class="row">
        {% for categorie in categories %}
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h6 class="card-title mb-0">
                            {% if categorie.nom == 'Incident' %}🚨
                            {% elseif categorie.nom == 'Panne' %}⚠️
                            {% elseif categorie.nom == 'Évolution' %}✨
                            {% elseif categorie.nom == 'Anomalie' %}🐛
                            {% else %}❓
                            {% endif %}
                            {{ categorie.nom }}
                        </h6>
                        <span class="badge bg-secondary">{{ categorie.tickets|length }} ticket(s)</span>
                    </div>
                    <div class="card-body">
                        {% if categorie.description %}
                            <p class="card-text">{{ categorie.description }}</p>
                        {% else %}
                            <p class="card-text text-muted">Aucune description</p>
                        {% endif %}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_admin_categories_edit', {id: categorie.id}) }}" class="btn btn-outline-primary btn-sm">
                            ✏️ Modifier
                        </a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endif %}
{% endblock %}
